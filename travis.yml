language: java
notifications:
 irc:
  channels:
   - "irc.esper.net#icbm"
  template:
   - "%{build_number} (%{branch} - %{commit}: %{commit_message} : %{author}): %{message} (%{build_url})"
  email: false
install: true
script:
  - gradle build
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk6
