language: java
cache:
  directories:
    - $HOME/.gradle
    - .gradle
sudo: false
notifications:
 irc:
  channels:
   - "irc.esper.net#icbm"
  template:
   - "%{build_number} (%{branch} - %{commit}: %{commit_message} : %{author}): %{message} (%{build_url})"
install: true
script:
  - gradle build --refresh-dependencies
jdk:
  - oraclejdk7
  - openjdk6
